alert("更新が成功しました。");

// 失敗メッセージの表示
alert("更新に失敗しました。");

// ポップアップメッセージの表示
window.alert("更新が成功しました。");

// フラッシュメッセージの表示
var flashMessageContainer = document.getElementById('flash-messages');
if (flashMessageContainer) {
  flashMessageContainer.innerHTML = "<div class='flash-success'>更新が成功しました。</div>";
}